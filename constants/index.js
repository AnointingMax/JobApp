import { COLORS, SIZES, calendarTheme } from "./theme";
import { Categories, addressList, workers, dates, tasks } from "./variables";

export {
  COLORS,
  SIZES,
  calendarTheme,
  Categories,
  addressList,
  workers,
  dates,
  tasks,
};
